var a;
((a = jQuery).fn.mauGallery = function (t) {
  t = a.extend(a.fn.mauGallery.defaults, t);
  var e = [];
  return this.each(function () {
    a.fn.mauGallery.methods.createRowWrapper(a(this)),
      t.lightBox &&
        a.fn.mauGallery.methods.createLightBox(
          a(this),
          t.lightboxId,
          t.navigation
        ),
      a.fn.mauGallery.listeners(t),
      a(this)
        .children(".gallery-item")
        .each(function (l) {
          a.fn.mauGallery.methods.responsiveImageItem(a(this)),
            a.fn.mauGallery.methods.moveItemInRowWrapper(a(this)),
            a.fn.mauGallery.methods.wrapItemInColumn(a(this), t.columns);
          var i = a(this).data("gallery-tag");
          t.showTags && void 0 !== i && -1 === e.indexOf(i) && e.push(i);
        }),
      t.showTags &&
        a.fn.mauGallery.methods.showItemTags(a(this), t.tagsPosition, e),
      a(this).fadeIn(500);
  });
}),
  (a.fn.mauGallery.defaults = {
    columns: 3,
    lightBox: !0,
    lightboxId: null,
    showTags: !0,
    tagsPosition: "bottom",
    navigation: !0,
  }),
  (a.fn.mauGallery.listeners = function (t) {
    a(".gallery-item").on("click", function () {
      t.lightBox &&
        "IMG" === a(this).prop("tagName") &&
        a.fn.mauGallery.methods.openLightBox(a(this), t.lightboxId);
    }),
      a(".gallery").on(
        "click",
        ".nav-link",
        a.fn.mauGallery.methods.filterByTag
      ),
      a(".gallery").on("click", ".mg-prev", () =>
        a.fn.mauGallery.methods.prevImage(t.lightboxId)
      ),
      a(".gallery").on("click", ".mg-next", () =>
        a.fn.mauGallery.methods.nextImage(t.lightboxId)
      );
  }),
  (a.fn.mauGallery.methods = {
    createRowWrapper(t) {
      t.children().first().hasClass("row") ||
        t.append('<div class="gallery-items-row row"></div>');
    },
    wrapItemInColumn(t, e) {
      var l;
      e.constructor === Number
        ? t.wrap(
            `<div class='item-column mb-4 col-${Math.ceil(12 / e)}'></div>`
          )
        : e.constructor === Object
        ? ((l = ""),
          e.xs && (l += " col-" + Math.ceil(12 / e.xs)),
          e.sm && (l += " col-sm-" + Math.ceil(12 / e.sm)),
          e.md && (l += " col-md-" + Math.ceil(12 / e.md)),
          e.lg && (l += " col-lg-" + Math.ceil(12 / e.lg)),
          e.xl && (l += " col-xl-" + Math.ceil(12 / e.xl)),
          t.wrap(`<div class='item-column mb-4${l}'></div>`))
        : console.error(
            `Columns should be defined as numbers or objects. ${typeof e} is not supported.`
          );
    },
    moveItemInRowWrapper(t) {
      t.appendTo(".gallery-items-row");
    },
    responsiveImageItem(t) {
      "IMG" === t.prop("tagName") && t.addClass("img-fluid");
    },
    openLightBox(t, e) {
      a("#" + e)
        .find(".lightboxImage")
        .attr("src", t.attr("src")),
        a("#" + e).modal("toggle");
    },
    prevImage() {
      let t,
        e = null,
        l =
          (a("img.gallery-item").each(function () {
            a(this).attr("src") === a(".lightboxImage").attr("src") &&
              (e = a(this));
          }),
          a(".tags-bar span.active-tag").data("images-toggle")),
        i = [],
        s =
          ("all" === l
            ? a(".item-column").each(function () {
                a(this).children("img").length &&
                  i.push(a(this).children("img"));
              })
            : a(".item-column").each(function () {
                a(this).children("img").data("gallery-tag") === l &&
                  i.push(a(this).children("img"));
              }),
          0);
      a(i).each(function (t) {
        a(e).attr("src") === a(this).attr("src") && (s = t);
      }),
        (t = i[(s = (s - 1 + i.length) % i.length)] || i[i.length - 1]),
        a(".lightboxImage").attr("src", a(t).attr("src"));
    },
    nextImage() {
      let t,
        e = null,
        l =
          (a("img.gallery-item").each(function () {
            a(this).attr("src") === a(".lightboxImage").attr("src") &&
              (e = a(this));
          }),
          a(".tags-bar span.active-tag").data("images-toggle")),
        i = [],
        s =
          ("all" === l
            ? a(".item-column").each(function () {
                a(this).children("img").length &&
                  i.push(a(this).children("img"));
              })
            : a(".item-column").each(function () {
                a(this).children("img").data("gallery-tag") === l &&
                  i.push(a(this).children("img"));
              }),
          0);
      a(i).each(function (t) {
        a(e).attr("src") === a(this).attr("src") && (s = t);
      }),
        (t = i[(s = (s + 1) % i.length)] || i[0]),
        a(".lightboxImage").attr("src", a(t).attr("src"));
    },
    createLightBox(t, e, l) {
      t.append(`<div class="modal fade" id="${
        e || "galleryLightbox"
      }" tabindex="-1" role="dialog" aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-body">
                            ${
                              l
                                ? '<div class="mg-prev" style="cursor:pointer;position:absolute;top:50%;left:-15px;background:white;"><</div>'
                                : '<span style="display:none;" />'
                            }
                            <img class="lightboxImage img-fluid" alt="Contenu de l'image affich\xe9e dans la modale au clique"/>
                            ${
                              l
                                ? '<div class="mg-next" style="cursor:pointer;position:absolute;top:50%;right:-15px;background:white;}">></div>'
                                : '<span style="display:none;" />'
                            }
                        </div>
                    </div>
                </div>
            </div>`);
    },
    showItemTags(t, e, l) {
      var i =
        '<li class="nav-item"><span class="nav-link active active-tag"  data-images-toggle="all">Tous</span></li>';
      a.each(l, function (t, e) {
        i += `<li class="nav-item active">
                <span class="nav-link"  data-images-toggle="${e}">${e}</span></li>`;
      }),
        (l = `<ul class="my-4 tags-bar nav nav-pills">${i}</ul>`),
        "bottom" === e
          ? t.append(l)
          : "top" === e
          ? t.prepend(l)
          : console.error("Unknown tags position: " + e);
    },
    filterByTag() {
      var t;
      a(this).hasClass("active-tag") ||
        (a(".active-tag").removeClass("active active-tag"),
        a(this).addClass(" active active-tag"),
        (t = a(this).data("images-toggle")),
        a(".gallery-item").each(function () {
          a(this).parents(".item-column").hide(),
            ("all" !== t && a(this).data("gallery-tag") !== t) ||
              a(this).parents(".item-column").show(300);
        }));
    },
  });
